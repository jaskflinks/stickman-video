services:
  - type: web
    name: stickman-video
    env: python
    pythonVersion: 3.9.18  # Full version required
    buildCommand: pip install -r requirements.txt
    startCommand: python render_video.py
    plan: free
